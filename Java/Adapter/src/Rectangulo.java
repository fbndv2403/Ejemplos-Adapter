public class Rectangulo {
    public void draw(int x, int y, int width, int height) {
        System.out.println("Rectangulo con punto de coordenadas de izquierda hacia abajo (" + x + ";" + y + "), ancho: " + width + ", largo: " + height);
    }
}
